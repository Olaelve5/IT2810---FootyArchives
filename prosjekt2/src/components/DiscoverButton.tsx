import classes from '../styles/Home/DiscoverButton.module.css';

export default function DiscoverButton() {
  return (
    <div className={classes.container}>
      <button className={classes.root}>Discover mathcups</button>
    </div>
  );
}
